# Minimum versions the code was written against, not a lock file.
#
# These are floors, so a fresh install will pick up whatever is current and may not reproduce
# the published run byte for byte. The exact environment of that run was not captured with
# `pip freeze` before the machine was released, which is the one gap in this project's
# reproducibility story and is recorded here rather than papered over. What the training log
# does establish is transformers 5.x, from a deprecation notice naming v5.2 as a future
# release, and the hardware and precision settings, which are printed at the top of every run.
#
# For a run that must be reproduced exactly, capture `pip freeze > requirements.lock`
# alongside the checkpoint.

torch>=2.0.0
# This floor read >=4.40.0 until the inference image was first built and run. The code calls
# from_pretrained(dtype=...), which is the 5.x signature -- on 4.x the argument is torch_dtype
# and the call raises TypeError before a single weight is loaded. So 4.40 was never a version
# this project could run on, and the header above already said the run used 5.x. The floor now
# matches the API the code actually uses.
transformers>=5.0.0
datasets>=2.18.0
peft>=0.10.0
bitsandbytes>=0.43.0
accelerate>=0.28.0
evaluate>=0.4.1
jiwer>=3.0.3
soundfile>=0.12.1
pandas>=2.0.0
numpy>=1.24.0
num2fawords>=0.0.1
scikit-learn>=1.2.0
tensorboard>=2.14.0
